name: release

on:
  push:
    branches: [master, main]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: echolang/setup-echo/.github/workflows/release.yml@v1
    with:
      changelog: true
